/**
 * GitHub OAuth App **Client ID** for the device flow (public; not a secret).
 * Set this once for your distribution so users never need a `.env` file.
 *
 * Create an OAuth app: https://github.com/settings/developers — callback URL can be
 * `http://localhost` (device flow does not redirect there).
 */
export const BUNDLED_GITHUB_OAUTH_CLIENT_ID = 'Ov23li7SvfrKXQGpfMom'
